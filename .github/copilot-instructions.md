# Copilot 指令

本專案的 AI 開發規範已統一至**根目錄的 `AGENTS.md`**（唯一事實來源）。

VS Code 已透過 `.vscode/settings.json` 的 `chat.useAgentsMdFile: true` 啟用，
Copilot 會自動讀取根目錄的 `AGENTS.md`，無需在此檔重複維護規範。

> 請勿在此檔新增實質規範內容，以免與 `AGENTS.md` 產生內容漂移。
> 所有規範變更請統一修改 `AGENTS.md`。
